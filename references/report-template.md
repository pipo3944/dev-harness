# Report: phase-XX <title>

> ワーカー → オーケストレーター への完了報告。「受け入れ条件の検証」は必須。
> オーケストレーターはこの report を読んで達成可否を判定し、人間ゲートにかける。

## サマリ
何をやったか3行以内。

## 変更点
- path:line — 何をどう変えたか
- ...

## 受け入れ条件の検証
- [x] 条件1 — どう確認したか（テスト/動作/コード根拠）
- [ ] 条件2 — 未達なら理由
- ...

## 遭遇した問題・判断
- 問題 → どう判断したか / なぜ

## 残課題・申し送り
- 次フェーズ or follow-up（なければ「なし」）

## 動作確認
> すべて**実測の出力**に基づく（未実行の結果を書かない。実行できなかったら理由つきで明記）。
> パイプに通さず素で実行し、コマンドごとに exit code と末尾の生出力を貼る。

- `<コマンド>` → exit `<code>`
  ```
  <末尾の生出力をそのまま貼る>
  ```
- ...
